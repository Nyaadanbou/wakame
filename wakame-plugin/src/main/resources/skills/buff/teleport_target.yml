type: teleport

teleportation:
  type: target

conditions:
  - type: molang
    id: "chance"
    eval: "math.random(0.0, 1.0) < 0.5"
    failure_message:
      - type: chat
        text: "Failed to teleport"