# 词条栏的重造设置
cells:
  # 这里每个节点的 key 对应一个词条栏的 id.
  # 如果定义了一个节点, 则允许重造该词条栏.
  # 反之, 未定义的词条栏不允许被重造.

  # 对应 id 为 a 的词条栏
  a:
    # 重造次数上限
    mod_limit: 3
    # 重造花费
    cost: 100.0

  # 对应 id 为 b 的词条栏
  b:
    mod_limit: 4
    cost: 150.0

  # 对应 id 为 c 的词条栏
  c:
    mod_limit: 5
    cost: 200.0