type: "kill_entity"

conditions:
  before_cast:
    - id: "chance"
      type: molang
      eval: "math.random(0.0, 1.0) < 0.5"
      failure_message:
        - type: chat
          text: "Failed to kill entity"