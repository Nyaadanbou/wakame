# 在这里设置收购站的物品 *渲染内容*.

# id: `custom_name`
custom_name:
  namespace: "general"
  # 可用标签
  # <value> 物品的 `minecraft:custom_name`
  tooltip: "<value>"

# id: `item_name`
item_name:
  namespace: "general"
  # 可用标签
  # <value> 物品的 `minecraft:item_name`
  tooltip: "<value>"

# id: `worth`
worth:
  namespace: "general"
  # 可用标签
  # <value> 物品的价值
  tooltip: "<!i>价值: <value>"
