# 设置收购站的物品 *渲染内容*.

# 物品组件
components:

  # ✔ 正常渲染
  custom_name:
    namespace: "component"
    tooltip: "<!i><value>"

  # ✔ 正常渲染
  item_name:
    namespace: "component"
    tooltip: "<value>"

  # ✔ 正常渲染
  lore:
    namespace: "component"
    # 额外标签
    # <line> 描述内容
    tooltip:
      line: "<!i><line>"
      header: [ ]
      bottom: [ ]
