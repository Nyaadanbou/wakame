# id: `durability`
durability:
  namespace: durability
  tooltip:
    - "[<durability>/<max_damage> 耐久度]"

# id: `repair_cost`
repair_cost:
  namespace: repair_cost
  tooltip:
    - "Repair will cost"
    - "- Gold: <value>"

# id: `repair_usage`
repair_usage:
  namespace: repair_usage
  tooltip:
    - "Click to repair it"
